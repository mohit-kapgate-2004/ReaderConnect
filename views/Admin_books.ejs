<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Manage Books</title>
    <style>
        body {
            font-family: 'Roboto', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #e0f7fa 0%, #b2ebf2 100%);
            min-height: 100vh;
        }
        .container {
            max-width: 1200px;
            margin: 30px auto;
            padding: 30px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        }
        h2 {
            text-align: center;
            color: #1e3a5f;
            margin-bottom: 20px;
        }
        .add-link {
            display: block;
            text-align: center;
            margin-bottom: 20px;
        }
        .add-link a {
            color: #1e3a5f;
            text-decoration: none;
            font-weight: 500;
        }
        .add-link a:hover {
            text-decoration: underline;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background: #1e3a5f;
            color: white;
        }
        tr:hover {
            background: #f5f5f5;
        }
        .action-buttons form {
            display: inline;
        }
        .action-buttons button {
            padding: 5px 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        .action-buttons .delete-btn {
            background: #dc3545;
            color: white;
        }
        .back-link {
            text-align: center;
        }
        .back-link a {
            color: #1e3a5f;
            text-decoration: none;
            font-weight: 500;
        }
        .back-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Manage Books</h2>
        <p class="add-link"><a href="/admin/books/add">Add New Book</a></p>
        <% if (books.length === 0) { %>
            <p style="text-align: center; color: #666;">No books available.</p>
        <% } else { %>
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Title</th>
                        <th>Author</th>
                        <th>Category</th>
                        <th>Available Quantity</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% books.forEach(book => { %>
                        <tr>
                            <td><%= book.id %></td>
                            <td><%= book.title %></td>
                            <td><%= book.author %></td>
                            <td><%= book.category %></td>
                            <td><%= book.available_quantity %></td>
                            <td class="action-buttons">
                                <form action="/book/delete/<%= book.id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this book?');">
                                    <button type="submit" class="delete-btn">Delete</button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
        <p class="back-link"><a href="/admin">Back to Dashboard</a></p>
    </div>
</body>
</html>